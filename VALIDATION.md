# 交付验证

验证平台：Windows x64、Python 3.13、Chrome。便携运行时为 Python 3.13.12。

| 验证 | 结果 |
| --- | --- |
| 后端测试 | 22 项通过：文件解析、DPAPI、配置恢复、连接错误脱敏、SSE 终态、取消、单任务互斥和复核保存 |
| SSE 前端测试 | 2 项通过：任意 UTF-8 分片、CRLF、心跳及中途断连 |
| 浏览器集成测试 | 4 项通过：示例、上传至复核、刷新恢复、下载、失败、取消和设置 |
| 布局截图 | 1440px、1920px、390px，以及报告、风险抽屉和设置视图 |
| 便携包 | 独立运行时导入、SDK CLI、Git Bash、中文空格路径、端口冲突、重复启动、停止重启均通过 |
| 离线界面资源 | 界面、字体、示例及报告预览均随包提供，无外部 CDN 依赖 |
| 包内容 | 未包含 `.git`、真实 `.env`、用户配置、测试材料和构建机器字节码缓存 |
| 运行依赖审计 | 前端生产依赖 npm audit 无已知漏洞 |

便携验证使用当前 Windows 主机上的限制 PATH、独立应用数据目录和独立用户配置环境，未在全新 Windows 虚拟机中验证。本机未提供模型凭据，真实模型调用尚未验证；自动化审查使用独立模拟 Agent 进程，不消耗模型额度。

测试脚本与命令见 README。截图与结构化便携验证结果位于被 Git 忽略的 `output/`；便携 ZIP 与 SHA256 校验文件位于 `dist/`。
