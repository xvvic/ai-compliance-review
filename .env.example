# ============================================================
# 环境变量配置示例
# 用法:把本文件复制成 .env,按需填写。
#   Windows:  copy .env.example .env
#   Mac/Linux: cp .env.example .env
# .env 已被 .gitignore 忽略,不会提交到仓库,可放心填真实密钥。
# ============================================================

# ── 鉴权(必需,二选一)────────────────────────────────
# 方式 A:直接用 Anthropic API Key(最省事,推荐给队友)
#   高校学生可在 https://console.anthropic.com 用学校邮箱申请免费额度
ANTHROPIC_API_KEY=

# 方式 B:把上面留空,改用 Claude 订阅账号登录(需付费套餐)
#   见 README 的「四、鉴权」章节,用 SDK 自带 CLI 执行 auth login

# ── Claude 模型(可选)──────────────────────────────────
# 不填     = 默认 Opus(最强、最慢)
# sonnet   = 均衡,审查步骤规划稳定(推荐)
# haiku    = 最快,但复杂任务/步骤规划不稳定
CLAUDE_CODE_MODEL=sonnet
# CLAUDE_CODE_FALLBACK_MODEL=haiku

# ── 接入国产模型(可选,想蹭免费/便宜额度看这里)──────────
# 原理见 README「六、接入其他模型」。用国产模型时:改用 ANTHROPIC_AUTH_TOKEN(不是上面的
# ANTHROPIC_API_KEY,两者别同时设),把上面 CLAUDE_CODE_MODEL 注释掉,改用 ANTHROPIC_MODEL。
# 去掉行首 # 四选一,填自己的 key(地址/模型名以各家官方文档为准):
#
# 智谱 GLM(BigModel;国际版 z.ai 地址换成 https://api.z.ai/api/anthropic):
# ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
# ANTHROPIC_AUTH_TOKEN=你的智谱APIKey
# ANTHROPIC_MODEL=glm-5.2
#
# DeepSeek:
# ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
# ANTHROPIC_AUTH_TOKEN=sk-你的DeepSeek密钥
# ANTHROPIC_MODEL=deepseek-v4-flash
#
# 通义千问(阿里云百炼):
# ANTHROPIC_BASE_URL=https://dashscope.aliyuncs.com/apps/anthropic
# ANTHROPIC_AUTH_TOKEN=sk-你的百炼APIKey
# ANTHROPIC_MODEL=qwen3-coder-plus
#
# 豆包(火山方舟 Coding Plan):
# ANTHROPIC_BASE_URL=https://ark.cn-beijing.volces.com/api/coding
# ANTHROPIC_AUTH_TOKEN=你的火山方舟APIKey
# ANTHROPIC_MODEL=doubao-seed-code-preview-latest
# CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1

# ── 北大法宝 MCP(可选)──────────────────────────────
# 不配也能跑:skill 会自动改用本地法律语料 legal_preference_txt/ 兜底。
# 要启用真实法条检索,从北大法宝控制台获取以下 6 个值(去掉行首 # 并填值):
# PKULAW_ACCESS_TOKEN=
# PKULAW_LAW_SEARCH_URL=
# PKULAW_LAW_KEYWORD_URL=
# PKULAW_CASE_SEMANTIC_URL=
# PKULAW_LAW_ITEM_URL=
# PKULAW_CITATION_VALIDATOR_URL=
